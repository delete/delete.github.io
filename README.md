# fellipe.me

My personal website — a single static page. No build step, no framework.

- `index.html` — the page
- `style.css` — the styles
- `netlify.toml` — Netlify publishes the repo root as-is

Deployed to [fellipe.me](https://fellipe.me) via Netlify.
